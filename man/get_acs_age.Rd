% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-acs-lmi.R
\name{get_acs_age}
\alias{get_acs_age}
\title{Get tidy ACS age and sex data}
\usage{
get_acs_age(
  year,
  survey = c("acs5", "acs1"),
  geography,
  ...,
  cache_table = TRUE
)
}
\arguments{
\item{year}{Numeric ACS vintage.}

\item{survey}{Either `"acs5"` or `"acs1"`.}

\item{geography}{A geography accepted by [tidycensus::get_acs()].}

\item{...}{Additional geography and geometry arguments forwarded to
[tidycensus::get_acs()]. Managed arguments such as `variables`, `table`,
and `output` cannot be supplied here.}

\item{cache_table}{Logical; cache Census metadata and downloaded tables.}
}
\value{
An `acs_lmi_bundle` with `total_population` and `race_ethnicity`
  tibbles.
}
\description{
Downloads the B01001 total-population table and the B01001A-I race and
ethnicity companion tables. The two structures are returned as separate
bundle components because their age bands differ.
}
\examples{
\dontrun{
age <- get_acs_age(2024, "acs5", "state", state = "MA")
age$total_population
age$race_ethnicity
}
}
